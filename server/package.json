{
  "name": "portfolio-tip-server",
  "version": "1.0.0",
  "private": true,
  "description": "Single serverless endpoint (Vercel) that creates a Stripe PaymentIntent for the portfolio app's exit-act tip flow. Test mode only.",
  "dependencies": {
    "stripe": "^22.3.1"
  },
  "devDependencies": {
    "@vercel/node": "^5.3.15",
    "typescript": "^5.9.2"
  }
}
